<%= form_with(model: suitdesign) do |form| %>
  <% if suitdesign.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(suitdesign.errors.count, "error") %> prohibited this suitdesign from being saved:</h2>

      <ul>
        <% suitdesign.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>
  <div class="field">
    <%= form.label :main_image %>
    <%= form.file_field :main_image %>
  </div>
  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
